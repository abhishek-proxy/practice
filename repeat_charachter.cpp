#include<iostream>
#include<string.h>
using namespace std;

void repeat_char(char str[50])
{   
    int len = 0;;
    len = strlen(str);
    char *pointer,*result ,ch;
    for(ch = 'a';ch<='z';ch++)
        for(c=0;c<len;c++)
        {


int main()
{
    char str[50];
    cin>>str;
    return 0;
}

